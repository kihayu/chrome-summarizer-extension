{
  "name": "article-summarizer",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun run bundler.ts",
    "dev": "bun run build --watch"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "turndown": "^7.2.0"
  }
}
